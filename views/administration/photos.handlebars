

<div class="container"><h1>{{title}}</h1></div>
{{#if loadingFinish}}
	<p style="color:green">Upload riuscito</p>
{{/if}}
<br />
<div id="exTab2" class="container">
<ul class="nav nav-tabs">
			<li class="active">
        <a  href="#1" data-toggle="tab">Dipinto sul pancione</a>
			</li>
			<li><a href="#2" data-toggle="tab">Feste</a>
			</li>
			<li><a href="#3" data-toggle="tab">Truccabimbi</a>
			</li>
      <li><a href="#4" data-toggle="tab">Quadri</a>
      </li>
		</ul>

			<div class="tab-content ">
			  <div class="tab-pane active" id="1">
          <form method="post" action="/administration/photos" enctype="multipart/form-data">
            <div class="form-group">
              <label>Scegli immagine</label>
              <input type="file" class="form-control" name="foto" placeholder="Foto" multiple="multiple">
              <input type="text" name="folder" value="pancione" hidden>
            </div>
            <button type="submit" class="btn btn-default">Invia</button>
          </form>

				</div>
				<div class="tab-pane" id="2">
          <form method="post" action="/administration/photos" enctype="multipart/form-data">
            <div class="form-group">
              <label>Scegli immagine</label>
              <input type="file" class="form-control" name="foto" placeholder="Foto" multiple="multiple">
              <input type="text" name="folder" value="feste" hidden>
            </div>
            <button type="submit" class="btn btn-default">Invia</button>
          </form>

				</div>
        <div class="tab-pane" id="3">
          <form method="post" action="/administration/photos" enctype="multipart/form-data">
            <div class="form-group">
              <label>Scegli immagine</label>
              <input type="file" class="form-control" name="foto" placeholder="Foto" multiple="multiple">
              <input type="text" name="folder" value="trucco" hidden>
            </div>
            <button type="submit" class="btn btn-default">Invia</button>
          </form>

				</div>
        <div class="tab-pane" id="4">
          <form method="post" action="/administration/photos" enctype="multipart/form-data">
            <div class="form-group">
              <label>Scegli immagine</label>
              <input type="file" class="form-control" name="foto" placeholder="Foto" multiple="multiple">
              <input type="text" name="folder" value="quadri" hidden>
            </div>
            <button type="submit" class="btn btn-default">Invia</button>
          </form>

				</div>
			</div>
  </div>
